<!DOCTYPE html>
<html lang="en">
<%- include("partials/head"); %>
<body>
  <%- include("partials/navbar"); %>
  <h1><%= title %></h1>
  <a href="<%= back.href %>"><%= back.text %></a>
  <main>
    <%- include("partials/errors.ejs") %>
    <form action="<%= locals.action || '/trainers/create' %>" method="POST">
      <label for="name">Name</label>
      <input type="text" name="name" id="name" value="<%= locals.trainerName %>" autocomplete="off" required>
      <button type="submit">Submit</button>
      <% if (!locals.trainerName) { %>
        <p>You can add pokemon after creating the trainer</p>
      <% } %>
    </form>
  </main>
</body>
</html>